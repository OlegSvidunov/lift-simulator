Java test task - lift simulator.

For running tests execute commands:

$ git clone https://github.com/OlegSvidunov/lift-simulator.git
$ cd lift-simulator/
$ bash ./gradlew clean test ('$ gradle clean test' if gradle already installed)



